# ==========================================
# CHECKPOINT 4 - PARTE 1
# Mercado Express - API com Spring Boot
# ==========================================
# IDE: IntelliJ IDEA
# Professor: Dr. Marcel Stefan Wagner
# Curso: TDS - FIAP
# ==========================================

# Porta do servidor Tomcat
server.port=${PORT:8082}

# ==========================================
# BANCO DE DADOS - H2 (para testes locais)
# ==========================================
spring.datasource.url=${DB_URL}
spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
spring.datasource.username=${DB_USERNAME}
spring.datasource.password=${DB_PASSWORD}

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# Console H2 (acessível via http://localhost:8082/h2-console)
spring.h2.console.enabled=true
spring.h2.console.path=/h2-console

# ==========================================
# BANCO DE DADOS - ORACLE FIAP (para produção)
# Descomente abaixo e comente o H2 para usar Oracle
# ==========================================
# spring.datasource.url=jdbc:oracle:thin:@//seu-servidor-oracle:1521/ORACLE_FIAP
# spring.datasource.driver-class-name=oracle.jdbc.OracleDriver
# spring.datasource.username=seu_usuario
# spring.datasource.password=sua_senha
# spring.jpa.hibernate.ddl-auto=none
# spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.OracleDialect

# ==========================================
# JACKSON
# ==========================================
spring.jackson.serialization.write-dates-as-timestamps=false
